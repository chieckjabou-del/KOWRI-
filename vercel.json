{
  "framework": "vite",
  "buildCommand": "pnpm --filter @workspace/kowri-app run build",
  "outputDirectory": "artifacts/kowri-app/dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
